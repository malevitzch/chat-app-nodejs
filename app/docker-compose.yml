services:
  frontend:
    ports:
      - "80:80"
    build: ./frontend
    env_file: .env
    networks:
      - main_network
  backend:
    build: ./backend
    env_file: .env
    networks:
     - main_network
networks:
  main_network:
    driver: bridge
